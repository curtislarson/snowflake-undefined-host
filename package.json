{
  "private": true,
  "name": "@quackware/snowflake-undefined-host",
  "description": "Snowflake Undefined Host",
  "author": "QuackWare, Inc.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest -c ./jest.unit.js",
    "compile": "tsc --build tsconfig.build.json",
    "clean": "rimraf \"packages/**/build\" && rimraf \"packages/**/node_modules\" && rimraf node_modules"
  },
  "devDependencies": {
    "@jest/types": "^27.0.6",
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.4",
    "typescript": "^4.3.5"
  },
  "engines": {
    "npm": ">= 7.17.0",
    "node": ">= 14.16.0"
  }
}
